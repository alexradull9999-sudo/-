meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  install: npm install
  command: npm run build
  artifacts:
    dist: dist

run:
  command: node dist/index.js
  containerPort: 80
